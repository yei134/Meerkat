import React, { useState } from "react";

const ConLeft = ({author, groups, createTime, modifiedTime, ownerOrg,title,datasetName }) => {

  return (
    <div className="conleft-container">
    <h1>DataSetName:{title}</h1>
    <div className="conleft">
       </div>
    </div>
  );
};

export default ConLeft;